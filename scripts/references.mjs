import fs from 'node:fs';
import {textbookContextLink} from './textbook-context.mjs';
const defs=JSON.parse(fs.readFileSync('book/definitions.json','utf8'));
const base='https://prl.korea.ac.kr/courses/cose212/2026/';
let glossary=`## How to read the links\n\nHighlighted terms throughout the notebook link here. Every entry gives an original plain-language definition, its connection to homework, and a direct link to the relevant **PDF page**. Most references point to the downloaded English textbook; topics added in the 2026 slides point to those slides explicitly.\n\n## Notation at a glance\n\n| Symbol | Read it as | In an implementation |\n|---|---|---|\n| ρ ⊢ e ⇒ v | Under runtime environment ρ, e evaluates to v | eval env expression |\n| ρ, M ⊢ e ⇒ v, M′ | Evaluation also transforms memory | eval env memory expression returns a pair |\n| Γ ⊢ e : T | Under type context Γ, e has type T | infer type_env expression |\n| x ↦ v | A mapping from x to v | A lookup-table entry |\n| Dom(M) | The allocated locations in memory M | Used to decide freshness |\n| [x ↦ v]ρ | Extend or shadow x in environment ρ | Prepend a binding when lookup chooses the first |\n| S(T) | Apply a type substitution throughout T | Recursive substitution helper |\n| FTV(T) | Free type variables of T | A set computed by traversing the type |\n| ∀α. T | A scheme universally quantifying α | Freshly instantiate α at each use |\n| S <: T | S can safely be used where T is expected | A subtype check, not unification |\n\nThe course uses different letters for environments and memory in different documents. In HW3, σ is the environment and M is memory. A symbol’s domain and codomain identify its role.\n\n`;
glossary+='## Choose the kind of question\n\n- **What does this symbol mean?** Start with the [syntax reference](syntax.html#same-symbol-different-job).\n- **How does the definition work in a program?** Each entry below links to its numbered textbook context and a focused explanation.\n- **Which assignment uses it?** Follow the [homework map](homework-map.html), where textbook and assignment numbers stay separate.\n\nThe evaluator names in the notation table are conceptual. Actual signatures differ: HW2 uses `eval expression environment`; HW3 uses `eval environment memory expression`. Read the template before copying a call.\n\n';
for(const d of defs){const source=d.source||'pl-book-eng.pdf';glossary+=`## ${d.term}\n\n${d.meaning}\n\n**Use it in:** ${d.homework}.\n\n${textbookContextLink(d.id)} · [Focused explanation](${d.chapter}) · [${d.source?'Lecture '+source.match(/lec(\d+)/)[1]:'English textbook'}, PDF p. ${d.page}](${base}${source}#page=${d.page})\n\n`;}
fs.writeFileSync('book/glossary.md',glossary);
const manifest=JSON.parse(fs.readFileSync('sources/manifest.json','utf8'));
let sources=`## Source snapshot\n\nCourse: [COSE212, Programming Languages, Fall 2026](${base}), Hakjoo Oh, Korea University. Retrieved **${manifest.retrieved_at.slice(0,10)}**. The English and Korean books are draft course textbooks by the instructor. This companion’s explanations and practice examples are independently written.\n\nThe course page exposes **${manifest.pdf_count} distinct PDF URLs**. **${manifest.downloaded_pdf_count} PDFs** downloaded successfully; their local archive preserves the course’s directory structure under \`sources/pdfs/\`. The public book links to the official originals. The [machine-readable inventory](assets/source-manifest.json) records sizes, hashes, page counts, URLs, and failed links.\n\n## Every linked PDF\n\n| Course file | PDF pages | Local archive status | Official source |\n|---|---:|---|---|\n`;
const files=manifest.files.filter(f=>f.url.endsWith('.pdf')).sort((a,b)=>a.file.localeCompare(b.file,undefined,{numeric:true}));
for(const f of files){const name=f.file.replace('sources/pdfs/','');sources+=`| ${name} | ${f.pages||'—'} | ${f.status===200?'Downloaded':'Unavailable · HTTP '+f.status} | [Open PDF](${f.url}) |\n`;}
sources+=`\n## Read the English textbook by concept\n\n| Companion topic | Textbook section | Direct PDF page |\n|---|---|---|\n| Induction | Chapter 1 | [11](${base}pl-book-eng.pdf#page=11) |\n| OCaml and recursion | Chapter 2 | [33](${base}pl-book-eng.pdf#page=33) |\n| Environments and evaluation | Chapter 3 | [97](${base}pl-book-eng.pdf#page=97) |\n| Functions and lexical scope | Chapter 4 | [119](${base}pl-book-eng.pdf#page=119) |\n| Functional language extension | Chapter 5 | [147](${base}pl-book-eng.pdf#page=147) |\n| State and parameter passing | Chapter 6 | [161](${base}pl-book-eng.pdf#page=161) |\n| Records and memory | Chapter 7 | [193](${base}pl-book-eng.pdf#page=193) |\n| Types and inference | Chapter 8 | [223](${base}pl-book-eng.pdf#page=223) |\n| Lambda calculus | Chapter 9 | [277](${base}pl-book-eng.pdf#page=277) |\n\nExceptions, the class interpreter, and typed classes have dedicated material in lectures 10, 11, and 19; they do not have matching standalone chapters in this English textbook snapshot. The [glossary](glossary.html) provides more precise definition-level links.\n\n## Source discrepancies to keep in mind\n\n- **Unavailable links:** \`slides/fixpoint.pdf\`, \`howtotryml.pdf\`, and the additional linked \`code/let.ml\` return HTTP 404. No substitute is presented as an original 2026 file. Fixed points remain covered in HW2 pp. 7–9 and lecture 20; the official GitHub repository provides local setup instructions.\n- **Lecture 11:** the course page links it both under classes and under static types. The PDF is titled “Classes and Objects”; static typing starts in lecture 12.\n- **Lecture 8 numbering:** it has 39 PDF pages; some slide footers retain an older total. All companion links use the PDF page index.\n- **HW2, p. 3:** the mutual-recursion call rule is visibly missing the result after its final evaluation arrow. Read its intended result as the value from the body premise. The print rule is schematic; the surrounding prose requires evaluating and printing the operand.\n- **HW2, p. 6:** example 3’s prose calls even on 13, while the supplied AST calls odd on 13 and the expected result is true. Trace the AST: odd(13) is true; even(13) is false.\n- **HW3, p. 1:** prose mentions read, but the provided AST contains no READ constructor. Its implementation contract is the actual listed AST.\n- **HW4, pp. 1–2:** it refers to ML− from HW3; in this course listing ML− is HW2, while HW3 is B. The type-checker AST matches HW2. Its brief handout leaves several typing-policy details unstated; see the [HW4 guide](hw4.html).\n- **Lecture 18, p. 13:** the procedure typing rule contains an inconsistent parameter-type symbol. The explanation uses the ordinary monomorphic parameter rule, distinguished from let generalization.\n\nThese observations concern the downloaded snapshot and are companion notes, not instructor-issued errata. The download script can be rerun to check for later source changes.\n\n## Attribution and further reading\n\nPrimary course sources: Hakjoo Oh’s [English textbook](${base}pl-book-eng.pdf), [Korean textbook](${base}pl-book.pdf), lecture PDFs, and homework handouts listed above. Official templates and environment: [kupl-courses/COSE212-2026fall](https://github.com/kupl-courses/COSE212-2026fall). Graphs are authored in [Mermaid](https://mermaid.js.org/config/usage.html) and rendered to SVG for reliable reading and printing.\n`;
const sourceGuide=`## Which source answers this question?\n\n| Question | Read first | Then use |\n|---|---|---|\n| What is the core definition? | [Textbook contents](textbook.html) and its linked PDF section | [Syntax](syntax.html) and [glossary](glossary.html) for individual symbols |\n| How do I think through a case? | The chapter's expandable Step by step explanation | A focused extra explanation for another example |\n| What must my homework implement? | Its handout and pinned starter interface | The [homework comparison map](homework-map.html#the-three-larger-assignments) |\n| Where are exceptions, objects, or subtyping? | Lectures 10, 11, and 19 | The explicitly labeled [integrated lecture extensions](textbook.html#lecture-to-chapter-map) |\n| Which commands run the examples? | [OCaml workbench](setup.html) and official environment README | The matching chapter download |\n\nThe archive status below describes the saved snapshot, not a live availability monitor. A starter contains unfinished code; an independent companion implementation is not an official answer or an interchangeable assignment template.\n\n`;
sources=sources.replace('## Every linked PDF',sourceGuide+'## Every linked PDF');
sources=sources.replace('## Source discrepancies to keep in mind','## Textbook and assignment differences\n\n- **Fun versus ML− equality:** the English book p.152 includes list equality and leaves nested details open. HW2 p.3 explicitly excludes list equality. See the [HW2 comparison](hw2.html#what-transfers-from-textbook-fun).\n- **Factorial:** the draft loop on p.78 starts multiplication at zero. The [Chapter 2 note](textbook-02.html#check-the-draft-s-factorial-boundaries) identifies the error and supplies a zero-safe recursive example.\n- **State interfaces:** the textbook download shares an engine across Chapters 6–7; its `SET` and evaluator mode differ from B\'s `ASSIGN` and public starter. See [HW3 adaptations](hw3.html#what-transfers-from-chapters-6-and-7).\n- **Checker results:** the textbook demo can enumerate scalar equality types, whereas HW4 returns one type. See [the declared HW4 policy](hw4.html#what-transfers-from-chapter-8).\n- **Object creation:** Lecture 11 p.24\'s formal freshness premise uses the store after argument evaluation, while its numbered prose lists allocation first. The [object guide](10-objects.html#object-creation-has-a-sequence) gives an implementation order that satisfies the premise and does not assume default field values.\n\n## Source discrepancies to keep in mind');
sources=sources.replaceAll('return HTTP 404','returned HTTP 404 at the recorded snapshot');
sources=sources.replace(/\| Chapter ([1-9]) \|/g,(_,n)=>'| [Chapter '+n+'](textbook-0'+n+'.html) |');
fs.writeFileSync('book/sources.md',sources);
console.log(`Generated reference pages for ${defs.length} definitions and ${files.length} PDF links.`);
